const todoBaseUrl = '/api/todo';
const todoIdUrl = '/api/todo/:id';
const todoUserIdUrl = '/api/todo/user/:userId';

const urls = {
  todoBaseUrl,
  todoIdUrl,
  todoUserIdUrl,
};

export default urls;
